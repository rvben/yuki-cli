name: upd dependency health

on:
  schedule:
    - cron: "26 6 * * 2"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: rvben/upd/.github/workflows/dependency-health.yml@2f773f6835a2dcd01cae0766bc15018345cd40dd # v0.6.2
    with:
      upd-version: v0.5.2
      min-age: 7d
      max-bump: minor
      fail-on-blocked: false
